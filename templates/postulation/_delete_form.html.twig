<form method="post" action="{{ path('app_postulation_delete', {'id': postulation.id}) }}" 
      onsubmit="return confirm('Are you sure you want to delete this item?');" class="delete-form">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ postulation.id) }}">
    <button type="submit" class="btn btn-danger">Delete</button>
</form>
